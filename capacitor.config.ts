import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.movinay',
  appName: 'movinay',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
